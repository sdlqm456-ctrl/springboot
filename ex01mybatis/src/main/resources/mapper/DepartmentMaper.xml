<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.example.demo.DepartmentMapper">
 
  <select id="selectAll" 
  resultType="DepartmentVO">
   select * 
    from departments 
    <where>
    <if test="departmentName != null">
    and department_name like '%' || #{departmentName} || '%'
    </if>
    </where>
  </select>
  
  <select id="selectOne">
  select * from departments
  where department_id = #{departmentId}
  </select>
  
  <insert id="insert">
   INSERT into departments(
   						   DEPARTMENT_ID, 
   						   DEPARTMENT_NAME, 
   						   MANAGER_ID, 
   						   LOCATION_ID)
   VALUES (
   			${departmentId}, 
   			#{departmentName}, 
   			#{managerID},
   			#{locationId})
  </insert>
  
  <update id="update">
   UPDATE departments 
	set department_id = #{departmentName}
	manager_id = #{managerId}
	where department_id = #{departmentId}
  </update>
  
  <delete id="delete">
    DELETE FROM departments
	WHERE deprtment_id = #{departmentId}
  </delete>
 
 </mapper>